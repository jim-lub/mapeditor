export default {
  properties: {
    mapSize: [20, 20], // columns in segments, rows in segments
    segmentSize: [512, 512], // width (pixels), height (pixels)
    tileSize: [32, 32] // width (pixels), height (pixels)
  }
}
