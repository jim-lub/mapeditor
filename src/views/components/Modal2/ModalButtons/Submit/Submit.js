import React from 'react';

import styles from 'buttons.module.css';

/**
* A Submit button
* @param {string} actionType ['click', 'submit'] choose if the button uses an onclick handler or should submit a form. Default: 'click'
* @param {function} action action to perform onClick
* @param {string} formName name of the form to submit on button submit
*/
export const Submit = ({ actionType, action, formName}) => {
  if (actionType === 'click') {

  }

  if (actionType === 'submit') {

  }

  return null;
}
