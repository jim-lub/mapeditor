import React from 'react';
import { connect } from 'react-redux';

const Tilesets = () => {
  return <div>Tilesets</div>
}

const mapStateToProps = (state) => {
  return {

  }
}

export default connect(mapStateToProps)(Tilesets);
